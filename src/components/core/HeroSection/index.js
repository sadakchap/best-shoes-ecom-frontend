import React from 'react';
import { HeroSectionWrapper } from './HeroElements';

const HeroSection = () => {
    return (
        <HeroSectionWrapper>
            {/* Carousel Section */}
            {/* Caategory section */}
        </HeroSectionWrapper>
    )
}

export default HeroSection
